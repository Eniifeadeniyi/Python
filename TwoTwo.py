rating = input('Enter an integer rating between 1 and 10')

#For the code to read integer into the variable, convert the type str to type int. Input always returns string.
#CORRECTION:
rating = int(input('Enter an integer rating between 1 and 10'))
